services:
  - type: web
    name: jordymac-racing-engine
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python automation/scheduler.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.1
